load(":generate-guard-tests.bzl", "generate_test_targets")

package(default_visibility = ["//visibility:public"])

# Upstream src/* packages retired 2026-09-23. Golden .d.ts files under
# tools/public_api_guard/{cdk,material,google-maps,youtube-player}/ are retained
# as historical reference only; no Bazel guard targets remain.
generate_test_targets(
    targets = [],
)
