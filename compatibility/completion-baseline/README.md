# Completion-plan baseline receipts (F00)

Immutable captures from task F00. These are **bootstrap** inputs and known-failure
receipts, not release passes.

| Directory | Line | Contents |
| --- | --- | --- |
| `f00-main-2026-09-23/` | main / 22.x | Git facts, toolchain, file-hash compare vs plan baseline, inspector/policy exits |
| `f00-l21-2026-09-23/` | 21.x worktree | Git facts, peers, install/pack attempts, inspector/policy exits |

Do not overwrite historical Sass seals with candidate output. Do not treat scanner
`exit=1` captures as successes. F01+ must bind gates to fresh artifact digests.
