# Historical unbound pack-proof tarball

Moved out of default test inputs by F00 (completion plan).

- Do **not** use this `.tgz` as a CI/smoke default.
- Bytes are preserved for traceability only; see `UNBOUND.json`.
- F01 must require a fresh build/pack and bind every gate to that digest.
