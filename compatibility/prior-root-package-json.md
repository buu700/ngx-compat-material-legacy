# Prior root package.json

The historical Angular Components root manifest (`name: components-srcs`) was replaced by the private workspace manifest (`ngx-material-legacy-workspace`) in the toolchain pin commit. Recover it from git history at `baseline/angular-components-16.2.x` / parent of that commit if needed for inventory. Material/CDK sources under `src/` were not deleted.
