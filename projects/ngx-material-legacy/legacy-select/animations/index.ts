/**
 * Optional Angular animation recipes for legacy select.
 * Primary select uses CSS keyframes; import this entry only for historical APIs.
 */
export {matLegacySelectAnimations} from './select-animations';
