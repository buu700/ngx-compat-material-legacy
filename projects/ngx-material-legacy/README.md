# @ngx-compat/material-legacy (library project)

Development library tree. Not published. Historical Material sources under
repository `src/` remain until inventory-gated extraction completes.

## Sass

Root facade: `_index.scss` (finite historical export map; owned M2 model under
`styles/`). Cyph and other Material-16 consumers should keep palette/theme
mixin spellings and only change the `@use` module URL.
